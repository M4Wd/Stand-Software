# Core app dependencies - needed everywhere (Pi or dev machine, any mode).
fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
websockets>=12.0

# Hardware dependencies - only importable/installable on a Raspberry Pi.
# Not needed to run in mock mode for development.
RPi.GPIO>=0.7.1; platform_machine.startswith("arm") or platform_machine.startswith("aarch64")
pigpio>=1.78; platform_machine.startswith("arm") or platform_machine.startswith("aarch64")
smbus2>=0.4.3; platform_machine.startswith("arm") or platform_machine.startswith("aarch64")
